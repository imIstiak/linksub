# Environment Variables for Database Configuration

# For Local Development (SQLite will be used automatically)
NODE_ENV=development

# Option 1: PostgreSQL Database
# NODE_ENV=production
# DATABASE_URL=postgresql://username:password@hostname:port/database

# Example PostgreSQL URLs:
# Neon: DATABASE_URL=postgresql://username:password@ep-hostname.region.neon.tech/dbname?sslmode=require
# Supabase: DATABASE_URL=postgresql://postgres:password@db.region.supabase.co:5432/postgres
# Railway: DATABASE_URL=postgresql://postgres:password@hostname.railway.app:5432/database

# Option 2: Google Sheets Database (Recommended for small projects)
GOOGLE_SHEET_ID=your_google_sheet_id_here
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----
Your private key here
-----END PRIVATE KEY-----"

# Note: For Vercel deployment, replace newlines in GOOGLE_PRIVATE_KEY with \n